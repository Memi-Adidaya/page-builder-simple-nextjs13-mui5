import type { PageStyles } from "src/types/types";

const defaultPageStyles: PageStyles = {
  padding: 16,
  margin: 16,
  minHeight: 1100,
  minHeightUnit: "px",
  maxHeight: 0,
  maxHeightUnit: "px",
};

export default defaultPageStyles